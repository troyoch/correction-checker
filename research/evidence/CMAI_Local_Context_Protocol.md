# Local context-path diagnostic

Prospective scope: execute the existing transcribed Evo memory-context function with synthetic dependency adapters. This is a separate offline diagnostic, not execution of CMAI v0.2's complete-Evo study. No API calls. Existing source files remain unchanged.

Use the M3 schedule correction (Friday to Tuesday). Plant current Tuesday in a structured record and superseded Friday in a stale summary. This deliberately constructed fixture tests context composition, not whether normal acquisition creates this state.

Before model evaluation, compare: both inputs, summaries removed, records withheld, both removed, and the global memory-enabled gate off. Expected contexts respectively contain both claims, Tuesday alone, Friday alone, neither, neither. Use a fixed clock and reset globals before every call. Save raw contexts and helper-call traces. Failure of an expectation is a diagnostic result; no model batch is authorized by this document.

The local candidate-fetch helper will return supplied fixture records. It does not reproduce WordPress database selection or vector retrieval. Thus removing records cannot distinguish retrieval from structured storage. Transcript removal is unavailable in this function. Developmental stores and complete system prompts are outside scope.
